import { AllAttendanceListView } from "../../../features/attendance";

export default function SuperAdminAllAttendancePage() {
  return (
    <AllAttendanceListView
      description="Every attendance record company-wide, with correction."
      portalLabel="Org Structure"
    />
  );
}
