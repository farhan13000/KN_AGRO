import { AllReportRequestsListView } from "../../../features/reportRequests";

export default function SuperAdminAllReportRequestsPage() {
  return (
    <AllReportRequestsListView
      description="Every report request company-wide — the ask-then-answer workflow, distinct from DSR."
      portalLabel="Org Structure"
    />
  );
}
